<head>
<style>
table, th, td {
    border: 2px solid black;
    padding: 8px;
}
tr:nth-child(even) {
    background-color: #dddddd;
}
</style>
</head>
<body>
<h1 style="text-align:center"> All Civilian Users: Admin Use Only </h1>
<h4>
<p style="padding-left: 20px;">
<%= link_to 'Back to profile', govtuser_path(current_user) %><br>
</p>
<table align="center" style="width:95%">
  <tr>
    <th style="width:25%">Username</th>
    <th style="width:20%">Name</th>
    <th style="width:25%" colspan="3"></th> 
  </tr>
 
  <% @civilusers.each do |user| %>
    <tr>
      <td><%= user.username %></td>
      <td><%= user.first_name %> <%= user.last_name %></td>
      <td style="text-align: center"><%= link_to 'Show', civiluser_path(user) %></td>
      <td style="text-align: center"><%= link_to 'Delete', civiluser_path(user),
				method: :delete,
				data: { confirm: 'Are you sure?'} %></td>
    </tr>
  <% end %>
</table>

</body>
</h4>